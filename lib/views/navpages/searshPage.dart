import 'package:flutter/material.dart';

class searshPage extends StatelessWidget {
  const searshPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('searshPage'));
  }
}